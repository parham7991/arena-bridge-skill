{
  "name": "arena-bridge-skill",
  "version": "5.1.0",
  "type": "module",
  "private": true,
  "description": "Arena Agent Bridge Skill — login to arena.ai with email/password and run real Agent sessions (multi-agent team-leader ready). OpenAI-compatible API.",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node src/index.mjs",
    "test": "node --test test/"
  }
}
